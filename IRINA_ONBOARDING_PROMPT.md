# Промт для Claude Code на макбуке Ирины

Скопируй всё ниже строки «——— START ———» и вставь первым сообщением в Claude Code у себя на маке. Дальше делай по шагам — Ирина, а не я, буду вести процесс.

---

——— START ———

Привет! Я Ирина, и я только что получила от Андрея готовый шаблон личного контент-ассистента в Telegram. Railway-проект уже создан, код задеплоен, GitHub-репо подключено. Осталось довести до рабочего состояния.

Помоги мне пройти оставшиеся шаги. Не торопись, спрашивай если что-то непонятно. **Что я НЕ должна делать сама:** не давай мне команд, которые требуют твоего вмешательства извне (например, я не буду открывать OAuth-ссылку Claude через твой браузер — я открою её в своём Chrome сама). **Что я должна делать строго сама:** создать Telegram-бота в @BotFather (это привязка к моему Telegram-аккаунту), пройти OAuth-логин Claude в своём браузере (привязка к моему устройству).

## Что мне от тебя нужно по порядку:

### Шаг 1. Проверь что у меня стоит на маке

Проверь наличие и версии:
- `node --version` (нужен 20+)
- `railway --version` (CLI Railway, любая 5.x)
- `claude --version` (Claude Code CLI)
- `gh --version` (GitHub CLI, опционально)

Если чего-то нет — поставь (`brew install node railway` и т.д.) и вернись сюда.

### Шаг 2. Залогинь меня в Railway

Выполни `railway login --browser`. Откроется мой браузер, я залогинюсь в свой Railway-аккаунт (тот, куда Андрей уже перенёс проект `prototype-ashet-dev`). Подтверди что залогинено как мой аккаунт.

### Шаг 3. Подключись к проекту

```bash
railway link --project prototype-ashet-dev
railway status
```

Покажи мне что подключились — я увижу URL `https://railway.com/project/<uuid>`. Запомним его.

### Шаг 4. Telegram-бот — это Я делаю сама

Этот шаг ты мне только описываешь, я делаю руками:

1. Открываю в Telegram `@BotFather`
2. `/newbot` → придумываю имя (например «Ашет — личный бренд») и username (например `ashet_irina_bot`, должен заканчиваться на `bot`)
3. Копирую токен вида `123456789:AAFxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
4. Присылаю токен сюда, в этот же чат

### Шаг 5. Поставь мой токен в Railway

Когда я пришлю токен:

```bash
railway variable set TELEGRAM_BOT_TOKEN=<токен> --skip-deploys
```

Затем напомни мне про другие ключи, которые нужно подставить (если Андрей ещё не сделал):
- `GROQ_API_KEY` — для распознавания голосовых (бесплатный, console.groq.com)
- `OPENAI_API_KEY` — для генерации картинок (платный, platform.openai.com)
- `RUNWAY_API_KEY` — для оживления фото в видео (платный, runwayml.com)

Я зарегистрирую их на СВОИХ аккаунтах с СВОЕЙ картой и пришлю сюда ключи.

### Шаг 6. Перезапусти деплой

После обновления Variables:

```bash
railway up -y -d -s prototype-ashet-dev
```

### Шаг 7. OAuth-логин Claude — это СТРОГО я сама

Это самый чувствительный шаг. Ты мне объясняешь — я делаю в своём Chrome.

1. Открой мне Railway Console одной командой: `railway open` (или дай прямую ссылку на `/console`)
2. В Console выполни:
   ```
   env HOME=/home/node su -p node -c 'claude --dangerously-skip-permissions'
   ```
3. Claude покажет welcome-экран (выбор темы) — нажму Enter чтобы пропустить
4. Потом покажет OAuth-ссылку вида `https://claude.ai/oauth/...`
5. **Эту ссылку я открываю СВОИМ Chrome, в той же вкладке, на своём макбуке**
6. Прохожу 2FA на своём телефоне
7. Принимаю экран «Bypass Permissions mode»
8. Жду пока Claude запустится в режиме `--channels`

Если что-то пойдёт не так — НЕ ПЫТАЙСЯ сам открыть ссылку. Скажи мне текст ошибки.

### Шаг 8. Telegram-плагин

После того как Claude запустится в Console, в той же Console выполни:

```
/plugin install telegram@claude-plugins-official
/reload-plugins
```

### Шаг 9. Telegram-пейринг

1. Открываю Telegram на своём телефоне
2. Нахожу своего бота (тот username, что создала в шаге 4)
3. Пишу ему «привет»
4. Бот присылает код пейринга вида `ABC123XYZ`
5. Присылаю код сюда
6. Ты в Console выполняешь:
   ```
   /telegram:access pair ABC123XYZ
   /telegram:access policy allowlist
   /exit
   ```

### Шаг 10. Проверка

После этого контейнер сам поднимется при следующем старте. Я пишу боту «привет, как дела?» — он отвечает.

---

## Чего НЕ делай

- Не открывай OAuth-ссылку сам — это заблокирует сессию навсегда (Anthropic привязывает к моему IP/устройству)
- Не проси меня отправить токены через Telegram-чат с Андреем — мне нужен безопасный канал (1Password Share / Bitwarden Send / onetimesecret.com)
- Не используй мой бот-токен для прямого вызова Telegram API — только через плагин
- Не запускай никаких AI-сервисов с моей карты без явного подтверждения суммы

---

## Когда всё заработает

Мне останется наполнить `CLAUDE.md` под себя — рассказать тебе кто я, чем занимаюсь, какой у меня стиль. Ты сделаешь мне 2-3 черновика, я выберу и мы запустим настоящую работу.

Поехали? Начни с шага 1.

——— END ———

---

## Как передать Ирине

1. **Сначала** — заархивируй папку с проектом (без `.env` если есть):
   ```bash
   cd /Users/andrejorlov/Documents/my-project
   tar czf prototype-ashet.tar.gz prototype-ashet/
   ```
2. **Transfer Railway-проекта** — Settings → Members → Transfer Ownership → email Ирины
3. **Transfer GitHub-репо** (опционально, можно оставить у себя и дать ей read-only доступ) — Settings → Danger zone → Transfer
4. **Отправь Ирине:**
   - Ссылку на этот файл (`IRINA_ONBOARDING_PROMPT.md`)
   - Или вставь текст промта прямо в сообщение
   - Railway-ссылку на проект (после transfer у неё в аккаунте)
   - GitHub-ссылку на репо (если transfer)

Ирина открывает Claude Code у себя на маке, вставляет промт — дальше он ведёт её по шагам.
